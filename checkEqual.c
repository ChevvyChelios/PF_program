#include<stdio.h>
int main(){
    int a = 2, b=2;

    // if (!(2^2))
    // if ((a & ~b) == 0)
    // if ((a - b) == 0)
    if ((2^3))
        printf("No\n");
    else
        printf("Yes\n");

    return 0;
}